<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>연락처 보기</title>
</head>
<body>
<h1>연락처 보기</h1>
<ul>
    {{#contact}}
        <li>
            <div>{{id}}</div>
            <div>{{name}}</div>
            <div>{{phoneNumber}}</div>
            <div>{{zipNumber}}</div>
            <div>{{email}}</div>
        </li>
        <a href="./modify?id={{id}}"><button>수정</button></a>
        <form id="delform" name="delform" method="post" action="./delete">
            <input type="hidden" name="id" value="{{id}}" />
            <input type="submit" value="삭제" />
        </form>
    {{/contact}}
    {{^contact}}
        <div>데이터가 없습니다.</div>
    {{/contact}}
</ul>
</body>
</html>